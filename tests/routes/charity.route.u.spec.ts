describe("Charity route tests", () => {
    test("the / endpoint", () => {
      // arrange
      const expectedResult = []
      // act
      const request = await router.
      // assert
    });
    test("the /:id endpoint", () => {
      expect(2 + 2).toBe(4);
    });
  });
  