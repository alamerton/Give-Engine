import router from "../../src/routes/charity.route";

describe("Charity route tests", () => {
  test("the / endpoint", () => {
    // arrange
    // const id = 1000671
    // const expectedResult = []
    // // act
    // // assert
    expect(2 + 2).toBe(4);
  });
  test("the /:id endpoint", () => {
    expect(2 + 2).toBe(4);
  });
});
