import { Box, Typography } from "@mui/material";

const Banner = () => {
  return (
    <Box sx={{ padding: "2rem", margin: "auto", width: "80%" }}>
      <Typography variant="h3">Welcome to the Give Engine Interface</Typography>
    </Box>
  );
};

export default Banner;
